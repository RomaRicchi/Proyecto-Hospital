doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Clínica Santa María

    // 👉 FontAwesome
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css")

    // 👉 Bootstrap
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css")

    // 👉 Tu CSS
    link(rel="stylesheet" href="/css/style.css")

  body
    // 👉 NAVBAR SUPERIOR (Logo + Usuario)
    nav.navbar.navbar-expand.navbar-dark.bg-primary.sb-topnav
      .container-fluid
        a.navbar-brand.ps-3(href="/")
          img(src="/img/logo5.png", alt="Logo", width="40", height="30", class="d-inline-block align-text-top me-2")
          | Clínica Santa María

        if typeof autenticado !== 'undefined' && autenticado
          button#sidebarToggle.btn.btn-link.btn-sm.order-1.order-lg-0.me-4.me-lg-0
            i.fas.fa-bars

        span.text-white.ms-auto.me-3.fw-semibold.d-none.d-md-block#reloj

        ul.navbar-nav.ms-auto.ms-md-0.me-3.me-lg-4
          li.nav-item.dropdown
            a.nav-link.dropdown-toggle(href="#", id="navbarDropdown", role="button", data-bs-toggle="dropdown", aria-expanded="false")
              i.fa-solid.fa-circle-user.me-1
              if autenticado
                span.d-none.d-md-inline Hola #{usuario.nombre}
              else
                span.d-none.d-md-inline Usuario

            ul.dropdown-menu.dropdown-menu-end(aria-labelledby="navbarDropdown")
              if !autenticado
                li
                  a.dropdown-item(href="/usuarios") Iniciar sesión
              else
                li
                  a.dropdown-item(href="/perfil") Mi perfil
                li
                  a.dropdown-item(href="/usuarios") Gestión de usuarios
                li
                  a.dropdown-item(href="/configuracion") Configuración del sistema
                li
                  hr.dropdown-divider
                li
                  a.dropdown-item(href="/logout") Cerrar sesión

    // 👉 CONTENIDO PRINCIPAL: Sidebar + Página
    .d-flex
      // 👉 Sidebar Vertical Izquierdo
      nav.d-flex.flex-column.bg-primary.p-3(style="min-height: 100vh; width: 250px;" id="sidenavAccordion")
        h4.text-white.mb-4
          i.fas.fa-hospital-user.me-2
          | Clínica Santa María

        a.nav-link.text-white(class=(currentPath == '/dashboard' ? 'active' : '') href="/dashboard" id="link-dashboard")
          i.fas.fa-tachometer-alt.me-2
          | Panel

        a.nav-link.text-white(class=(currentPath == '/pacientes' ? 'active' : '') href="/pacientes" id="link-pacientes")
          i.fas.fa-user-injured.me-2
          | Pacientes

        a.nav-link.text-white(class=(currentPath == '/internaciones' ? 'active' : '') href="/internaciones" id="link-internaciones")
          i.fas.fa-procedures.me-2
          | Internaciones

        a.nav-link.text-white(class=(currentPath == '/admisiones' ? 'active' : '') href="/admisiones" id="link-admisiones")
          i.fas.fa-bed.me-2
          | Admisiones

        a.nav-link.text-white(class=(currentPath == '/camas' ? 'active' : '') href="/camas" id="link-camas")
          i.fas.fa-bed.me-2
          | Habitaciones


      // 👉 Contenido principal de cada página
      main.p-4.flex-grow-1
        block content

    // 👉 Scripts
    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")
    script(src="/js/script.js")
